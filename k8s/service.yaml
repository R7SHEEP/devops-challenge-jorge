apiVersion: v1
kind: Service
metadata:
  name: devops-api-service
spec:
  type: NodePort
  selector:
    app: devops-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30007